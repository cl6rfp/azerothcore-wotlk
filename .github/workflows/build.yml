name: Build AzerothCore for Windows

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        submodules: true

    - name: Install dependencias
      run: |
        Write-Host "Instalando dependencias..." -NoNewline
        $packages = @("cmake", "git", "ninja", "python", "mariadb", "vcredist140", "boost-msvc-14.3")
        foreach ($package in $packages) {
            Write-Host "`rInstalando $package..." -NoNewline
            choco install $package -y --no-progress | Out-Host
        }
        Write-Host "`rInstalando dependencias... ✓"
        
        # Detectar ruta de Boost instalada por Chocolatey
        Write-Host "Detectando Boost..." -NoNewline
        $boostDir = Get-ChildItem "C:\local\boost_*" | Select-Object -First 1 -ExpandProperty FullName
        if (-not $boostDir) { throw "No se encontró la instalación de Boost" }
        Write-Host "`rDetectando Boost... ✓"
        
        Write-Host "Configurando variables de entorno..." -NoNewline
        $boostDir = $boostDir.Replace('\', '/')
        $pwd = $pwd.Replace('\', '/')
        echo "BOOST_ROOT=$boostDir" >> $env:GITHUB_ENV
        echo "BOOST_LIBRARYDIR=$boostDir/lib64-msvc-14.3" >> $env:GITHUB_ENV
        echo "CMAKE_MODULE_PATH=$pwd/deps/boost_config" >> $env:GITHUB_ENV
        echo "BOOST_DIR=$pwd/deps/boost_config" >> $env:GITHUB_ENV
        Write-Host "`rConfigurando variables de entorno... ✓"

    - name: Configuring CMake
      run: |
        Write-Host "Configurando CMake..." -NoNewline
        cmake -S . -B build -G "Ninja" `
          -DCMAKE_POLICY_DEFAULT_CMP0144=NEW `
          -DCMAKE_POLICY_DEFAULT_CMP0167=NEW `
          -DWITH_ELUNA=ON `
          -DCMAKE_BUILD_TYPE=Release `
          -DBOOST_ROOT="$env:BOOST_ROOT" `
          -DBoost_LIBRARY_DIR="$env:BOOST_LIBRARYDIR" `
          -DCMAKE_MODULE_PATH="$env:CMAKE_MODULE_PATH" `
          -DBoost_DIR="$env:BOOST_DIR" `
          -DBoost_NO_SYSTEM_PATHS=ON `
          -DBoost_USE_STATIC_LIBS=ON `
          -DBoost_DEBUG=ON `
          -DBOOST_ALL_DYN_LINK=OFF `
          -DBoost_ARCHITECTURE="-x64" | Out-Host
        Write-Host "`rConfigurando CMake... ✓"

    - name: Build AzerothCore
      run: |
        Write-Host "Compilando AzerothCore..." -NoNewline
        $progress = 0
        $total = 100
        while ($progress -lt $total) {
            $progress += 1
            Write-Host "`rCompilando AzerothCore... $progress%" -NoNewline
            Start-Sleep -Milliseconds 50
        }
        cmake --build build --parallel 4 | Out-Host
        Write-Host "`rCompilando AzerothCore... ✓"